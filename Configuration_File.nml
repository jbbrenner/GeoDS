!Configuration file

!________________________________________________________________________________________________________________________________________________________________________________!
!_______________________________________________________Parametrization regarding inputs_________________________________________________________________________________________!


!_______________________________________________________!
!________General inputs parametrization_________________!
&Global_inputs_variables

lr_monthly_climate_data_availibility = .TRUE.				     !Variable used to check whether monthly climate input data are available or not
lr_climate_data_file = "/home/jbrenner/These/Cartes/lr_surf_temp.nc"  	     !Path of the low resolution temperature input netCDF file (GCM's outputs). Note that the input data must be first interpolated on the high resolution elevation grid
lr_climate_data_x_size = 71                                                  !Dataset extent in the x spatial dimension (x/longitude) for the selected variable
lr_climate_data_y_size = 61                                                  !Dataset extent in the y spatial dimension (y/latitude) for the selected variable
lr_climate_data_t_size = 1200                                                !Dataset extent in the t temporal dimension for the selected variable
/

!_______________________________________________________!
!________Parametrization regarding temperature__________!

&Temperature

!Input variables
lr_surface_temperature_id = "ts"				     !Name of the variable to be read in the input file 
/

!_______________________________________________________!
!________Parametrization regarding topography___________!

&Topography

lr_elevation_file = "/home/jbrenner/These/Cartes/lr_elevation_file.nc" !Path of the low resolution elevation input netcdf file. Note that the input data must be first interpolated on the high resolution elevation grid
lr_surface_elevation_id = "elevation"
hr_elevation_file = "/home/jbrenner/These/Cartes/Topographie_Test_1km.nc" !Path of the high resolution elevation input netCDF file (GCM's outputs)
hr_surface_elevation_id  = "S"						  !Name of the variable to be read in the input file
hr_topo_x_size = 71							  !Dataset extent in the x spatial dimension (x/longitude) for the selected variable
hr_topo_y_size = 61							  !Dataset extent in the y spatial dimension (y/latitude) for the selected variable
hr_topo_t_size = 1							  !Dataset extent in the t temporal dimension for the selected variable (for topography, dim(t)=1)
/


!________________________________________________________________________________________________________________________________________________________________________________!
!__________________________________________________________Parametrization regarding outputs_____________________________________________________________________________________!

&Downscaled_outputs

ds_monthly_climate_data_file = "/home/jbrenner/These/GeoDS_Outputs/monthly_downscaled_temperature_Alpes_1km.nc" !Path of the downscaled climate data file generated by the algorithm
ds_annual_climate_data_file = "/home/jbrenner/These/GeoDS_Outputs/annual_downscaled_temperature_Alpes_1km.nc"
ds_x_grid_lower_bound = -443750.d0									!High resolution topography file's lower bound in x
ds_y_grid_lower_bound = -251600.d0									!High resolution topography file's lower bound in y
spatial_resolution = 1000.d0										!Size of the outputs grid points			
ds_annual_data_generation = .TRUE.
/



